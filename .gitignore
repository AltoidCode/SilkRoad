# Node.js dependencies
node_modules/              # Node.js modules (should not be committed)
npm-debug.log*             # npm debug logs
yarn-debug.log*            # yarn debug logs
yarn-error.log             # yarn error log
package-lock.json          # Optional: lock file if you use npm (use yarn.lock if using yarn)
yarn.lock                  # If you use Yarn, you should commit yarn.lock instead

# Build directories
build/                     # React build folder (frontend build artifacts)
dist/                      # TypeScript build output (backend and frontend build files)
public/                    # Public folder in React (images, icons, etc. should remain)
*.log                      # Log files

# Environment files
.env                       # Don't commit .env files (store sensitive info here)
.env.local                 # Local environment variables for frontend/backend
.env.production            # Production environment variables for frontend/backend
.env.development           # Development environment variables for frontend/backend

# Frontend (React/CRA)
# React/CRA generated files
/.cache/                   # Cache files created by React
coverage/                  # Code coverage reports (e.g., Jest)
*.tsbuildinfo              # TypeScript build info file

# IDE and editor folders
.vscode/                   # Visual Studio Code settings
.idea/                     # JetBrains IDE settings (e.g., WebStorm, PhpStorm)
.DS_Store                  # macOS Finder settings
Thumbs.db                  # Windows thumbnail cache

# Operating system files
Desktop.ini                # Windows system file
*~                         # Backup files (ending with ~)
*.swp                      # Vim swap files

# Docker files
docker-compose.override.yml  # Local overrides for Docker (optional)
Dockerfile.local             # Local-specific Dockerfile (if applicable)

# Backend (NestJS)
# Database files and models (do not commit local database files)
*.sqlite                   # If using SQLite database for local development
*.env                      # Ignore any other .env files (e.g., .env for database config)

# Logs and runtime files
logs/                       # Log files
*.log                       # Any log files (e.g., server logs)
*.pid                       # Process IDs
*.seed                      # Seed files

# Tailwind CSS configuration (if used)
tailwind.config.js
postcss.config.js

# Storybook build (if using Storybook)
storybook-static/

# Generated by IDEs and text editors
*.sublime-workspace         # Sublime Text workspace files
*.sublime-project           # Sublime Text project files

# Ignore test reports and files
coverage/                  # Code coverage reports (from testing)
test-report/               # Custom test reports

# Frontend (Next.js if applicable)
.next/                     # Next.js build artifacts
out/                       # Static export folder for Next.js

# Compiled code files from frontend build systems
*.js                       # JavaScript files generated from source
*.jsx                      # JSX files generated from source
*.ts                       # TypeScript files that were compiled
*.tsx                      # TypeScript JSX files that were compiled
